import { BnLazyBgDirective } from './bn-lazy-bg.directive';

describe('BnLazyBgDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLazyBgDirective();
    expect(directive).toBeTruthy();
  });
});
