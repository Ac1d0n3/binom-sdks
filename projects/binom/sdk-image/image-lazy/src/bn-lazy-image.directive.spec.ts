import { BnLazyImageDirective } from './bn-lazy-image.directive';

describe('BnLazyImageDirective', () => {
  it('should create an instance', () => {
    const directive = new BnLazyImageDirective();
    expect(directive).toBeTruthy();
  });
});
